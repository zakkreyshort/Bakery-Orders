@{
    Layout = "_Layout";
}

@using Bakery.Models;

<h1>Vendors</h1>

@if(Model.Count == 0)
{
    <h3>No vendors added quite yet!</h3>
}

@foreach (Vendor vendor in Model)
{
    <h3><a href='/vendors/@vendor.Id'>@vendor.Name</a></h3>
}